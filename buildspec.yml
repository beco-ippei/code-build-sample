version: 0.2

phases:
  # install:
  #   runtime-versions:
  #     docker: 18
  build:
    commands:
      - echo Build started on `date`
      - echo Building the Docker image...
      - export myvar=(a b c)
      - for item in $myvar; do echo $item; done
      - echo End process ----
